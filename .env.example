# Copy this file to .env and fill in your Anthropic API key.
# Used by the open-by-voice intent fallback.
# WARNING: VITE_ variables are bundled into client-side code at build time.
# For local/personal builds only. Use a scoped/rate-limited key.
VITE_ANTHROPIC_API_KEY=
